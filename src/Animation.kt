class animation(frameLength : Int, framesArray : Array<String>){
    var framesOfAnim : Int = frameLength

    var frames = framesArray

    fun playAnim(){
        var outputString : String
        for(i in 0..framesOfAnim){
            outputString = frames[i]

        }
    }
}